<h1><%= @message %></h1>
<p>このページは <code>HomeController#index</code> → <code>app/views/home/index.html.erb</code> で表示されています。</p>
<p>流れ: <strong>Route → Controller → View</strong></p>
<p><a href="<%= memos_path %>">メモ一覧へ（段階4: Model を使う CRUD）</a></p>
